import React, { PropTypes } from 'react'

// Category.propTypes = {
//   users: PropTypes.array
// }

const Group = ({ users }) => {
    return (
      <div>
      
      </div>
      )
}

export default Group